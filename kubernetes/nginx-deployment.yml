apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  labels:
    component: web
    app: app

spec:
  template:
    metadata:
      name: nginx-pod
      labels:
        component: web
        app: app
    spec:
      containers:
        - name: nginx-container
          image: nginx:alpine
  replicas: 8
  selector:
    matchLabels:
      app: my-app
      component: web